<Window x:Class="CirCalc.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CirCalc"
        mc:Ignorable="d"
        Title="HelpWindow" Height="450" Width="800">
    <Grid Background="LightGray" Margin="10">
        <Label Content="CirCalc Help" HorizontalAlignment="Center" Margin="0,23,0,0" VerticalAlignment="Top" FontSize="18"/>
        <RichTextBox Height="233" VerticalAlignment="Top" Margin="22,75,28,0" FontSize="16" Cursor="None">
            <FlowDocument>
                <Paragraph>
                    <Run Text="This is a simple tool with which you can calculate the diameter, surface area and circumference of a circle from a radius.  The radius can be entered as either a whole number or a decimal number.
                         
                         
                         You can also choose a color to fill the circle which will be drawn on the screen to the right."/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
        <Button x:Name="CloseBtn" Content="Close" HorizontalAlignment="Left" Margin="22,328,0,0" VerticalAlignment="Top" FontSize="14" Width="72" Click="CloseBtn_Click"/>

    </Grid>
</Window>
